name: skill-finder-ledger
skill: skill-finder
purpose: |
  Iteration ledger for skill-finder.
# 2026-07-24: catalog を agent-kit 現存78本へ全面再構築し、SKILL.md の壊れ参照
# (superpowers/chezmoi/nix-setup/cloudflare-deploy 等)と rejection-log の
# 前提スタックを現存 skill へ直したため、旧前提の実行履歴をリセットした。
# 自動 iterate は使用保留(reseen≠resolved の誤収束)。waxa-eval 稼働時に
# one-shot 評価 + 人間承認でゼロから積み直す。
iterations: []
