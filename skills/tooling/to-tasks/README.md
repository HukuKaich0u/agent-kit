<!-- apm:readme:begin (generated by scripts/gen-skill-readme.rb — do not edit this block) -->
# to-tasks

> Split a small, current-session implementation into a 2–5 step ephemeral task list without creating a design record or publishing tracker work. Use when work benefits from an ordered breakdown but does not need multi-session, multi-agent, or durable tracking; do not use to create issues.

## Install

### APM (recommended)

```bash
apm install HukuKaich0u/agent-kit/skills/tooling/to-tasks
```

Or declare it in `apm.yml` for a reproducible setup:

```yaml
dependencies:
  apm:
    - HukuKaich0u/agent-kit/skills/tooling/to-tasks
```

then run `apm install`.

### Any agentskills.io-compatible agent

This is an [Agent Skills](https://agentskills.io) skill — a portable
`SKILL.md` folder that works with Claude Code, Codex, opencode, Gemini CLI,
Cursor, and other [compatible agents](https://agentskills.io/clients).

With the [`skills`](https://github.com/vercel-labs/skills) CLI (installs into
every detected agent's skills directory):

```bash
npx skills add HukuKaich0u/agent-kit --skill to-tasks
```

Or copy the folder in directly:

```bash
npx degit HukuKaich0u/agent-kit/skills/tooling/to-tasks ~/.claude/skills/to-tasks
```

See [SKILL.md](./SKILL.md) for what this skill does and when to use it.
<!-- apm:readme:end -->
