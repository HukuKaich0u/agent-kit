interface:
  display_name: "To Tasks"
  short_description: "Create a current-session task breakdown"
  default_prompt: "Use $to-tasks to split this small implementation into an ordered task list without creating issues."
policy:
  allow_implicit_invocation: false
